嗨同学B，我们的主程序底层While循环骨架已经搭好，需要你接管其中的get_system_prompt()和sliding_window_manager(messages)两个接口 
。你的核心任务是编写严谨的System Prompt并实现一套上下文滑动窗口算法 
。标准非常关键，你设计的系统提示词必须包含严格的少样本提示，约束大模型必须按照Thought、Action、Action Input、Observation、Final Answer的规范交替输出，不能乱了格式 
，且get_system_prompt()函数必须返回规范的纯字符串 。请在网页端对提示词进行多轮人工测试，确保模型能稳定输出指定格式后 
，在第三周前将代码交付给我 。  

嗨同学C，主程序的工具调度入口call_tool(action_name, action_input_raw)已经为你预留好了 。你需要使用原生Python编写计算器、简单网络搜索和本地文本文件读写这3个独立的工具函数 
，并在你的tools.py里写好分发路由，把大模型传进来的参数正确分发给对应的工具。组长验收硬性标准是：无论工具执行成功还是失败，所有工具函数的输入和输出都必须是规范的字符串 
。你必须使用unittest编写测试用例，模拟边界情况，当本地函数执行崩溃时必须捕获异常并优雅地返回包含错误信息的字符串，这样主循环才能把错误作为Observation弹回给大模型让其自纠错 
。另外，联调测试阶段你需要负责运行main.py来完整抓取并保存多步推理日志提交给同学E ，同时建立一个“AI提示词垃圾桶”记录报错与AI修复过程 

嗨同学D，底层的ReAct Agent引擎类已经封装完毕，你不需要修改核心循环逻辑。你需要在你的前端代码文件中直接导入我写好的NativeReActAgent类，当用户在网页端点击提交时直接调用agent.run(user_input)即可 
。为了满足你在Gradio或Streamlit界面上流式显示大模型实时Thought和Action思考链的需求 ，我会把Agent类中的messages历史列表动态暴露出来，或者配合你进行标准输出print的重定向捕获，让你能在网页面板上实时渲染。录制3分钟Demo演示视频时 
，建议重点展示Agent的自纠错和多步推理过程 ，并利用剪映等工具的AI功能快速配音和生成字幕 

嗨同学E，作为我们小组的学术交付统筹者 ，我会为你全力提供技术素材支持。为了方便你撰写《实验报告》和制作答辩PPT 
，我会为你提供核心While循环调度状态机的系统架构图及文字思路说明 。等我和同学B、C在第三周联调成功后 
，我会把终端打印出来的多步推理真实日志完整复制给你 ，作为报告和PPT里证明项目跑通的核心依据 。撰写报告时，请务必找同学C索要他在测试阶段记录的“AI提示词垃圾桶”报错与AI修复过程记录 
，将其作为工程实践亮点写进去 
